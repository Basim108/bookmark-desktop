# fit-grid-cells-within-canvas

Compute grid capacity from the space cells actually consume (gap + padding) so no cell is clipped at the canvas edge, and distribute leftover space into the cells themselves
