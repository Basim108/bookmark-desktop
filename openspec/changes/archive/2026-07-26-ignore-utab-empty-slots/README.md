# ignore-utab-empty-slots

Treat a uTab bookmark entry with no url as an empty grid slot rather than a
failed import: drop it silently, do not count it as skipped, and keep it out of
the import report
